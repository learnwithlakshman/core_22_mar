spring:
  datasource:
    username: db_user
    password: dbuser@123
    url: jdbc:postgresql://localhost:5432/lwl_db

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  servlet:
    context-path: /iplstat
  port: 8090